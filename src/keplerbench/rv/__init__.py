"""Radial-velocity model and the bridge into RadVel.

This package exists only for Work Plan steps 4 and 5 (error propagation and
the Monte Carlo comparison). The solver benchmark itself does not depend on
it, so a broken radvel install cannot block the rest of the team.

Owner: Fariha.
"""
